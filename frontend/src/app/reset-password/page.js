import ResetPasswordPageSection from "../../sections/ResetPasswordPage/ResetPasswordPageSection";

export default function ResetPasswordPage({ params }) {
  return <ResetPasswordPageSection params={params} />;
}
